extends template.pug

block pagecontent
    form(action="/createpost", method="post")
        p
            input(class="form-control", type="text", name="title" placeholder="Title...")
        p
            input(class="form-control", type="text", name="creator" placeholder="Creator...")
        p
            input(class="form-control", type="text", name="content" placeholder="Content...")
        p
            input(class="btn btn-default", formaction="/newpost", type="submit", value = "Submit")
    
